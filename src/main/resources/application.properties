server.port=${PORT:8080}
spring.application.name=Foodies

spring.mongodb.uri=${DB_URI}
spring.mongodb.database=${DB_DATABASE}

#GitHub imagerep
github.username=Sakthivel1107
github.repo=image-storage
github.branch=main
github.token=${GITHUB_TOKEN}

jwt.secret.key=helloiamsakthivel.Viamcurrentlypursingbe

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

server.tomcat.max-swallow-size=10MB

logging.level.org.springframework.boot.autoconfigure.mongo=DEBUG
logging.level.org.springframework.data.mongodb=DEBUG

#razorPay configuration
razorpay_key=${RAZARPAY_KEY}
razorpay_secret=${RAZARPAY_SECRET}

#mail configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${SPRING_MAIL_USERNAME}
spring.mail.password=${SPRING_MAIL_PASSWORD}
spring-mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true


